# Validate an hla_genotype object

Validate an hla_genotype object

## Usage

``` r
validateHlaGeno(x)
```

## Arguments

- x:

  An object to validate.

## Value

\`TRUE\` if the object is a valid \`hla_genotype\` object, otherwise
throws an error.
